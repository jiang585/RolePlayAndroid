# 多人角色扮演群聊 App —— 需求文档

> 项目：`RolePlayChat`
> 版本：v1.0（MVP 需求基线）
> 文档性质：完整详细需求文档（本次交付范围仅为本文档，不含代码实现）
> 默认语言：中文界面，支持多语言对话内容

---

## 1. 项目概述

### 1.1 背景与目标
开发一款单机 Android 应用，让用户体验「沉浸式多人角色扮演群聊」：用户在一个类**微信群聊**的界面中，与多个由 AI 扮演的角色（NPC）在同一个群里交流。用户可以选择自己在剧情中的身份（主角/配角/任意），设定世界观与多个角色卡，AI 负责让角色"活起来"——生成符合角色的发言、动作和表情。

### 1.2 核心价值
- 把「写作 / 跑团 / 剧情共创」变成一个轻量的群聊，降低门槛。
- 全本地优先，隐私数据不出设备（仅对话调用云端大模型 API）。
- 高度可装扮（气泡、背景），可导入导出分享，支持二次创作。

### 1.3 关键术语
| 术语 | 说明 |
| --- | --- |
| 群组 / 剧本 | 一个完整的角色扮演会话 = 世界观 + 若干角色（含玩家身份）+ 聊天记录 + 外观装扮。 |
| 角色 / NPC | 由 AI 扮演的群内成员，拥有独立头像、姓名、设定。 |
| 玩家身份 | 用户在该群里扮演的具体角色（主角/配角/自定义）。 |
| 角色卡 | 描述一个角色的结构化数据（姓名、头像、性格、背景、说话风格……）。 |
| 系统提示消息 | 群聊中居中的动作/表情/旁白描述，无头像、无气泡（区别于角色消息）。 |
| 世界观 | 整个剧本的世界背景设定（时代、地点、规则、势力、主线线索等）。 |
| 剧本包 | 可选的整体导出包：可包含世界观 + 全部角色卡 + 聊天记录。 |

---

## 2. 整体范围与边界

### 2.1 本次（v1.0 / MVP）范围
- 单机运行，AI 扮演所有其他 NPC（"多人"指的是一个群里存在多个 AI 角色，非真实联网多人）。
- 支持一个或多个群组（剧本），每个剧本可独立管理世界观、角色、装扮与聊天记录。
- 玩家始终在本机以**一个身份**参与（我固定右侧显示），可随时切换为不同的角色身份（主角/配角/任意）后发言。
- 支持 NPC 被 AI 编排发言（谁接话、说几句由 AI 决定），并支持"AI 自动推进多轮"（玩家沉默时 NPC 自演）。
- 支持动作/表情由 AI 自动生成，渲染为居中系统消息。
- 角色卡支持结构化导入（JSON + 头像图）与纯文本描述让 AI 完善两种方式。
- 数据本地存储（Room 数据库 + 本地文件），支持角色卡 / 世界观 / 聊天记录各自独立导出与导入。
- 气泡装扮、背景图更换。

### 2.2 明确不做（Non-Goals / v1.0 排除）
- 不实现真实多人联网聊天（无服务器 / 无同步）。
- 不做本地离线大模型（仅云端 API）。
- 不做语音通话、视频、红包、表情包商店等服务型功能。
- 不做付费、账号体系、云同步（无登录）。API Key 由用户自行配置并存储于本机。
- 不做角色扮演内容的强审查之外的用户聊天内容审核系统（AI 能力由云端 API 决定）。

---

## 3. 用户角色与使用场景

### 3.1 目标用户
- 剧情写作者、跑团（TRPG）爱好者、同人创作者、希望体验沉浸式聊天陪伴的普通用户。

### 3.2 关键使用场景（User Stories）
1. **创建剧本**：用户想开一个"宫斗"或"原创奇幻"故事 → 新建剧本 → 输入世界观一句话描述 → 让 AI 完善成完整世界观。
2. **建立角色**：用户想有 5 个角色 → 逐个人物简单描述（或导入角色卡）→ AI 完善为完整角色卡 → 选择/上传头像、设置姓名与角色卡样式。
3. **选择身份进场**：用户在剧本里选"我是主角（某角色）"或"我是配角/旁观者" → 进入群聊。
4. **群聊推进**：用户说出自己的台词 → AI 编排 NPC 依次/择优回应，并在需要时插入动作表情系统消息。
5. **让 NPC 自演**：用户点击"AI 推进"→ 连续生成若干轮 NPC 之间的交流。
6. **装扮**：用户更换群聊背景图、调整气泡样式与每角色颜色。
7. **分享**：用户导出角色卡/世界观/聊天记录，发给他人导入复用。

---

## 4. 功能需求

功能分为以下模块：**剧本管理、角色管理、世界观设定、群聊引擎、AI 对话、个性化装扮、数据导入导出、应用设置**。

### 4.1 剧本管理（Group / Script）
| 编号 | 需求 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-101 | 剧本列表 | 首页展示已创建的剧本（卡片），含名称、封面/背景缩略图、角色数、最近聊天时间。 | P0 |
| FR-102 | 新建剧本 | 填写剧本名 + 世界观一句话描述（可选），可在此后进入完善。 | P0 |
| FR-103 | 编辑剧本元信息 | 修改名称、一句话简介、封面图、外观风格。 | P1 |
| FR-104 | 剧本详情 | 进入剧本后展示：世界观概览、角色成员列表、进入群聊入口、装扮入口。 | P0 |
| FR-105 | 删除剧本 | 删除剧本（二次确认），级联删除其聊天记录与本地资源。 | P1 |
| FR-106 | 多剧本 | 支持并行维护多个剧本，互不影响。 | P0 |

### 4.2 世界观设定（World Setting）
| 编号 | 需求 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-201 | 世界观编辑 | 可编辑结构化的世界观字段（见数据模型），支持富文本换行。 | P0 |
| FR-202 | AI 完善世界观 | 输入关键词/一段话 → 调 AI 生成完整世界观草稿 → 用户可编辑后保存。 | P0 |
| FR-203 | 世界观生成可视化 | 维护关键设定条目（时代/地点/势力/规则/主线），可在角色 AI 对话中作为上下文注入。 | P1 |
| FR-204 | 世界观版本管理 | 记录世界观修改历史，可回退（v1.0 可选）。 | P2 |

### 4.3 角色管理（Character / NPC）
| 编号 | 需求 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-301 | 角色列表 | 剧本下所有角色，展示头像、姓名、简要设定。 | P0 |
| FR-302 | 新建角色 | 两种入口：①仅文字描述 + AI 完善；②导入结构化角色卡。 | P0 |
| FR-303 | 角色卡字段 | 姓名、头像（可上传/默认生成/从角色卡导入）、别名、性别/年龄、性格、背景故事、说话风格、口头禅、特长/缺陷、与其他角色的关系、示例台词、隐藏设定（对 AI 生效对玩家可选隐藏）。 | P0 |
| FR-304 | AI 完善角色卡 | 基于用户简单描述或导入残缺卡，AI 补全缺失字段并给出可编辑内容。 | P0 |
| FR-305 | 编辑角色 | 任意字段可改；头像可更换，可从相册选择、拍照、或由 AI 生成头像提示词（具体生成依赖云端，可选）。 | P0 |
| FR-306 | 删除/停用角色 | 删除角色后其历史消息保留但标记为"已离开"；或将角色移出群（不删除历史）。 | P1 |
| FR-307 | 每角色外观 | 每个角色可单独指定气泡样式与颜色（见 4.6）与发言昵称颜色。 | P1 |

### 4.4 身份选择（玩家身份）
| 编号 | 需求 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-401 | 选择当前身份 | 进入剧本聊天前/聊天中，用户可把"我"设定为：主角（指定某个角色作为我的代表身份）、配角、或"任意/meta 旁观者"。 | P0 |
| FR-402 | 身份角色绑定 | 若绑定某个角色卡，则我发言时以该角色身份显示（右侧气泡为该角色外观），AI 把他当作真实角色对待。 | P0 |
| FR-403 | 切换身份 | 聊天过程中可切换我的身份（随时更换所扮演的角色卡），历史消息保持原样。 | P1 |
| FR-404 | 旁观者模式 | 我选择"任意/旁观"时，我的发言以系统旁观/叙述形式出现，不属于任何 NPC。 | P1 |

### 4.5 群聊引擎（Group Chat）
| 编号 | 需求 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-501 | 类微信群聊界面 | 顶部为群名与成员、底部为输入框（含发送、AI 推进、快捷动作），中间滚动消息列表。 | P0 |
| FR-502 | 消息类型 | 角色发言（气泡）、系统提示消息（动作/表情/旁白，居中无气泡）、日期分隔、系统公告（进群/退群等）。 | P0 |
| FR-503 | 我固定右侧 | 以我当前身份发出的消息靠右显示（微信习惯），AI/NPC 消息靠左显示。 | P0 |
| FR-504 | 玩家发言逻辑 | 玩家发送文本即为其所扮演角色的台词。 | P0 |
| FR-505 | 发送动作 | 玩家可手动输入动作（可选），渲染为系统提示消息（居中）。 | P1 |
| FR-506 | AI 编排 NPC 发言 | 玩家发言后，AI 决定哪些 NPC 接话、顺序与条数，逐个/批量生成。 | P0 |
| FR-507 | AI 自动推进多轮 | 玩家点击"AI 推进"后，AI 连续生成多轮纯 NPC 交流（参数：轮数）。可随时停止。 | P0 |
| FR-508 | 停止生成 | 流式生成过程中可取消当前批次。 | P0 |
| FR-509 | 消息持久化 | 所有消息入库、可选滚动分页加载。 | P0 |
| FR-510 | 加载与滚动 | 进入自动滚到底部；上滑加载更早消息。 | P1 |

### 4.6 个性化装扮（Customization）
| 编号 | 需求 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-601 | 背景图更换 | 群聊背景：内置若干默认背景 + 用户从相册选择本地图片（支持拉伸/平铺/暗化透明度）。 | P0 |
| FR-602 | 气泡样式库 | 内置多套气泡样式（圆角大小、边框、填充方式、箭头方向）。 | P0 |
| FR-603 | 每角色气泡颜色 | 可为每个角色分别设定气泡背景色、文字色、昵称颜色。 | P0 |
| FR-604 | 全局气泡设置 | 可设默认气泡样式与颜色，未单独设置的角色套用全局。 | P1 |
| FR-605 | 系统消息样式 | 系统提示消息的灰色居中样式与字号可微调。 | P2 |
| FR-606 | 主题间切换 | 装扮设置即时生效并持久化。 | P1 |

### 4.7 数据导入导出
| 编号 | 需求 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-701 | 导出角色卡 | 将单个角色卡导出为文件（JSON + 可选内嵌头像图，或外部图路径打包）。 | P0 |
| FR-702 | 导出世界观 | 将世界观导出为文件。 | P1 |
| FR-703 | 导出聊天记录 | 导出当前群全部/选定范围的聊天记录（文本 or JSON）。 | P1 |
| FR-704 | 导出剧本包 | （可选增强）一键导出世界观 + 全部角色卡 + 聊天记录为单一打包文件（zip）。 | P2 |
| FR-705 | 导入角色卡 | 支持从文件导入角色卡（JSON/标准第三方格式兼容，见 4.7.1）。 | P0 |
| FR-706 | 导入世界观 | 支持从文件导入世界观。 | P1 |
| FR-707 | 导入聊天记录 | 支持从文件导入聊天记录（合并或覆盖，用户选择）。 | P2 |
| FR-708 | 分享渠道 | 借助系统分享（分享文件），并支持"从文件/相册导入"。 | P1 |

#### 4.7.1 角色卡导入格式
- **首选**：RoB / 类 TavernAI 风格的角色卡（字段：`name`, `description`, `personality`, `first_mes`, `mes_example`, `scenario`, `system_prompt`, `char_greeting`, `avatar` 等）。
- **自定义 JSON**：本项目约定一套字段映射（见数据模型），向下兼容缺失字段。
- **纯文本**：直接粘贴已有角色的文字设定，走"AI 完善"流程。
- 头像：内嵌 base64 / 独立图片文件 / 外部 URL（导入时下载本地化）。

### 4.8 应用设置（Settings）
| 编号 | 需求 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-801 | API 配置 | 配置 OpenAI 兼容接口的 BaseURL、API Key、默认模型名、Temperature 等。本地加密存储（Android Keystore）。 | P0 |
| FR-802 | 对话参数 | 最大输出 token、top_p、每轮上下文长度策略（自动裁剪）。 | P2 |
| FR-803 | 默认装扮 | 默认背景与气泡。 | P2 |
| FR-804 | 数据管理 | 清空某群聊天、清空全部数据。 | P1 |

---

## 5. 页面（Screen）清单

> 页面均采用 **Java + XML** 布局，Material Components 主题。

| 页面 | 名称 | 关键内容 | 关联功能 |
| --- | --- | --- | --- |
| S1 | 剧本列表 | 剧本卡片网格/列表、新建按钮、设置入口 | FR-101~106 |
| S2 | 新建/编辑剧本 | 名称、一句话、封面、外观 | FR-102~103 |
| S3 | 剧本详情 | 世界观概览卡、角色成员列表、进入群聊、装扮；顺带世界观编辑入口 | FR-104, 201~204, 301 |
| S4 | 世界观编辑 | 结构化字段编辑 + 「AI 完善」按钮 | FR-201~204 |
| S5 | 角色列表 / 详情 | 查看与编辑角色；添加 NPC / 选身份 | FR-301~307 |
| S6 | 角色卡选择器（导入） | 文件选择、粘贴文本、导入预览、映射确认 | FR-702, 705 |
| S7 | 身份选择 | 列出可选角色卡（主角/配角/旁观）+ 确认 | FR-401~404 |
| S8 | 群聊 | 主界面（核心） | FR-501~510, 601~605 |
| S9 | 装扮设置 | 背景、气泡、颜色、系统消息样式 | FR-601~606 |
| S10 | 设置 | API、对话参数、数据管理 | FR-801~804 |

### 5.1 群聊界面（S8）详细规格
- **顶部栏**：群名 + 成员数点击展开成员抽屉；右侧进入装扮/设置。
- **消息区**：
  - 角色消息：左侧为 NPC，右侧为我（角色卡气泡 + 昵称 + 头像，可点击看到角色卡）。
  - 系统提示消息：水平居中，灰色小字，无头像无气泡（如"（张三轻轻叹了口气，望向窗外）"）。
  - 日期分隔、公告。
- **底部输入区**：文本输入框；发送按钮；"AI 推进"按钮（带轮数弹层）；快捷动作按钮。
- **流式渲染**：AI 生成时逐字显示，可停止。
- **背景**：可设置背景图，置于消息区的底层 Drawable。

---

## 6. 数据模型

### 6.1 实体（Room 表）
```
Script(剧本)
  id, name, oneLine, worldId, coverRes/coverPath, customStyleId,
  createdAt, updatedAt, sortIdx

WorldSetting(世界观)
  id, scriptId(FK), era, location, factions(text), rules(text),
  storyHook, backgroundFull(text), tags[], versionNote

Character(角色)
  id, scriptId(FK), name, alias[], avatarRes/avatarPath, gender,
  age, personality, backstory, speakingStyle, catchphrases[],
  strengths[], flaws[], relationships[], sampleLines[],
  hiddenSetting(可对玩家隐藏),
  bubbleStyleId, bubbleColor, textColor, nickColor, systemPrompt,
  sortIdx, enabled

SessionMember(群成员关系)
  id, scriptId(FK), characterId(FK, nullable), isPlayerSlot,
  playerRoleType(主角/配角/旁观) 

Message(消息)
  id, scriptId(FK), characterId(FK, nullable), type(CHAR/NARRATION/EVENT/DATE),
  side(MINE/THEIR) 冗余便于渲染, content, seq, createdAt,
  status(streaming/done/cancelled/failed), meta(json)

Appearance(装扮)
  id, scope(script/global), bgType(builtin/image), bgPath, bgTone/alpha,
  bubbleStyleId, defaultBubbleColor, defaultTextColor, fontSize

Setting(设置)
  key, value(加密 json)

ExportManifest / ImportLog(可选，导入导出元数据)
```

### 6.2 角色卡 JSON（自定义约定，示例）
```json
{
  "name": "林晚晴",
  "aliases": ["晚晚"],
  "gender": "女",
  "age": "21",
  "personality": "温柔但嘴硬……",
  "backstory": "……",
  "speakingStyle": "说话带着书卷气，偶尔用成语",
  "catchphrases": ["……", "……"],
  "strengths": ["观察力强"],
  "flaws": ["害怕改变"],
  "relationships": {"张三": "青梅竹马"},
  "sampleLines": ["（笑了笑）你又来啦。"],
  "avatar": "base64... 或 /path",
  "bubbleColor": "#FFFFFF",
  "__meta": {"source": "tavern|rpc", "version": 1}
}
```

---

## 7. AI 对话与编排协议

### 7.1 接入方式
- 使用 **OpenAI 兼容** 的 `/v1/chat/completions` 接口（HTTPS）。
- 支持流式（`stream=true`）逐字渲染。
- 通过 `BaseURL / API Key / 模型名 / 温度 / 最大token` 配置（设置页，Android Keystore 加密存储）。

### 7.2 系统 Prompt 组装（为每个剧本动态生成）
将以下内容拼装为 system 提示，注入已整理的有限上下文（避免超长）：
1. 世界观全文摘要。
2. 群内全部角色的角色卡摘要（姓名 + 关键性格/说话风格/关系/目标）。
3. 玩家当前身份的角色卡（若为旁观者则注明）。
4. 可选的导演/叙事指令（语气、长度、语言中文等）。

### 7.3 输出能力约定
AI 需要能生成两种"产物"：
- **角色发言**：正常台词文本。
- **系统叙事**（动作/表情/旁白）：包装为结构化消息，例如：
  ```
  {"narrations": ["（张三把咖啡推到你面前，笑了）"], 
   "turns": [{"character": "林晚晴", "content": "趁热喝，别整凉了。"}]}
  ```
  采用**结构化函数调用（tool_call / function calling）或专用 JSON 输出**，由客户端解析后分别渲染为系统提示消息与角色气泡。解析失败时降级为纯文本按行列显示。

### 7.4 回合编排
- 玩家发言后，一次请求返回"本回合所有 NPC 行为"（多个 `turns` + `narrations`），客户端按序渲染。
- 支持「AI 推进」：连续多次请求生成多轮 NPC 互动，直到用户停止或达到轮数上限。
- 上下文窗口管理：保留最近 N 条消息 + 长期意图（如隐藏设定）始终在 system 中，超出部分旧的裁剪。

### 7.5 流式体验
- `stream=true`，SSE 解析，逐字追加到当前气泡或系统消息。
- 生成状态在数据库中标记，崩溃/中断可恢复或重试。

### 7.6 错误与降级
- 网络失败 → 提示重试；Key 无效/超额度 → 引导到设置。
- 内容解析失败 → 原始文本回退显示。

---

## 8. 非功能需求（NFR）

| 类别 | 要求 |
| --- | --- |
| 性能 | 群聊流畅滚动（分页加载）；流式渲染不卡顿；头像图片本地缓存。 |
| 兼容 | 支持 Android 8.0（API 26）+；适配深色/浅色模式（或可关闭）。 |
| 安全隐私 | API Key 用 Keystore 加密；共享偏好不存明文 Key；聊天内容本地明文存储（本地应用），导出由用户主动进行；权限：相册/相机/存储按需申请（Android 13+ 采用新权限模型）。 |
| 离线 | 除 AI 请求外，浏览已保存内容、装扮、导入导出均可离线。 |
| 可用性 | 中文界面；操作路径短；错误有引导。 |
| 可维护性 | 模块分层（data/domain/ui），Manual DI，可测试。 |

---

## 9. 技术选型建议

| 层 | 方案 | 说明 |
| --- | --- | --- |
| 语言/UI | Java + Jetpack View + XML | 用户明确要求 Java，用经典 View 体系保持一致。 |
| 架构 | MVVM（ViewModel + LiveData）+ Repository + Manual DI（Hilt 可选，v1.0 可用手写 DI 减少复杂度） | 分层清晰、可测试。 |
| 数据库 | Room + SQLite | 持久化剧本/角色/消息/装扮/设置。 |
| 导航 | Navigation Component | 页面路由。 |
| 网络 | OkHttp + Retrofit | 调 OpenAI 兼容 API；自定义 Converter 解析结构化输出。 |
| 流式 | OkHttp + 自定义 SSE 解析（或 SSE 库） | 逐字渲染。 |
| 图片 | Glide | 头像/背景加载与缓存；位图处理、暗化。 |
| 加密 | Android Keystore + EncryptedSharedPreferences / EncryptedFile | Key 与敏感数据加密。 |
| 主题 | Material Components for Android | 传统 View 主题与控件。 |

---

## 10. 验收标准（关键场景）

- AC-01 新建剧本并让 AI 完善世界观 → 可见结构化世界观并保存。
- AC-02 通过文字描述创建角色，AI 补全角色卡 → 展示完整角色卡并可编辑。
- AC-03 导入一个第三方角色卡 JSON（含头像）→ 生成可参与聊天的角色。
- AC-04 选择"我是主角"，在群聊发送台词 → 我以该角色身份右侧气泡显示。
- AC-05 我发言后，AI 生成 ≥1 个 NPC 回复并可能穿插居中系统提示消息 → 渲染符合规格（NPC 左、我右、系统居中无气泡）。
- AC-06 点击"AI 推进"→ 连续生成多轮 NPC 交流并可停止。
- AC-07 流式回复逐字出现；生成中可取消。
- AC-08 更换背景图与气泡颜色 → 即时生效且重启后保留。
- AC-09 导出角色卡/世界观/聊天记录 → 生成文件；(对角色卡) 新建群导入 → 能复用。
- AC-10 配置 API Key 后运行对话；Key 错误时给出可操作的引导而非崩溃。
- AC-11 在 Android 8.0+ 真机/模拟器上完成以上流程，无明显卡顿与崩溃。

---

## 11. 里程碑建议（供后续排期参考，本次仅记录）

- **M1（P0 核心）**：剧本/角色/世界观管理 + 群聊界面 + OpenAI 兼容接入（含流式、回合编排、系统消息）+ 本地持久化 + 基础装扮（背景/气泡颜色）+ 角色卡文本导入 + API 设置。
- **M2（P1 增强）**：结构化角色卡导入导出 + 世界观/聊天导出 + AI 推进多轮 + 每角色气泡样式 + 身份切换 + 停止生成优化。
- **M3（P2 打磨）**：剧本包打包导入、主题系统、数据清理与版本、深色模式、丰富内置装扮。

---

## 12. 待确认 / 开放问题
- 角色头像是否允许"AI 生图"（依赖额外图像 API）？v1.0 建议仅支持上传/选择，AI 生图后置。
- 是否需要"隐藏设定（对玩家隐藏仅对 AI 生效）"——已列入 FR-303 为可选，是否 v1.0 就做？
- 剧本最多角色数、单条消息最大长度是否需要上限（建议各给默认值，如 20 个角色 / 每条 4096 字符）。
- 是否需要自动记忆跨群共享？v1.0 建议按群隔离。
